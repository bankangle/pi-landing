# Deployment (adapter-node)
# ORIGIN must be the public URL for CSRF checks when forms exist; kept for reference.
# Behind a reverse proxy: PROTOCOL_HEADER/HOST_HEADER/ADDRESS_HEADER are set in the Dockerfile.
